const Validate = require('../validate.js');

describe('Validate', () => {

  // TODO: Write a test that checks if isPassword() returns false when the password is less than 8 characters.
  it('should return false when the password is less than 8 characters', () => {)
  // TODO: Write a test that checks if isPassword() returns false when the password does not contain at least 1 uppercase.
  it('should return false when the password does not contain at least 1 uppercase', () => {)
  // TODO: Write a test that checks if isPassword() returns false when the password does not contain at least 1 lowercase.
  it('should return false when the password does not contain at least 1 lowercase', () => {)
  // TODO: Write a test that checks if isPassword() returns false when the password does not contain at least 1 number.
  it('should return false when the password does not contain at least 1 number', () => {)
  // TODO: Write a test that checks if isPassword() returns true when the password is at least 8 characters long and contains an uppercase, lowercase, and number.
  it('should return true when the password is at least 8 characters long and contains an uppercase, lowercase, and number', () => {)  
  // TODO: Write a test that checks if isPassword() returns false when the password does not contain at least 1 number.
  it('should return false when the password does not contain at least 1 underscore', () => {)
  // TODO: Write a test that checks if isPassword() returns false when the password does not contain at least 1 dash.
  it('should return false when the password does not contain at least 1 dash', () => {)
  // TODO: Write a test that checks if isPassword() returns false when the password contains more than 1 underscore.
  it('should return false when the password contains more than 1 underscore', () => {)
  // TODO: Write a test that checks if isPassword() returns false when the password contains more than 1 dash.
  it('should return false when the password contains more than 1 dash', () => {)
  // TODO: Write a test that checks if isPassword() returns false when the password contains more than 1 uppercase.
  it('should return false when the password contains more than 1 uppercase
  // TODO: Write a test that checks if isPassword() returns true when the password is at least 8 characters long and contains an uppercase, lowercase, and number.
});
